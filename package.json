{
  "name": "www",
  "version": "1.0.0",
  "main": "src/index.pug",
  "repository": "git@github.com:koeotravel/www",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "parcel build src/pages/",
    "start": "parcel src/pages/index.pug",
    "deploy": "yarn build && now dist"
  },
  "devDependencies": {
    "pug": "^2.0.3"
  }
}
